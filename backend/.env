# MongoDB connection string (use MongoDB Atlas free tier or local)
MONGO_URI=mongodb+srv://myuser:myuser09@cluster0.tazzpon.mongodb.net/?appName=Cluster0

# JWT secret for signing auth tokens
JWT_SECRET=replace_this_with_a_long_random_string

# Port the server runs on
PORT=5000

# Seat hold duration in minutes (how long a seat stays "held" before auto-release)
SEAT_HOLD_TTL_MINUTES=10

# Waitlist offer duration in minutes (how long a waitlisted customer has to complete booking)
WAITLIST_OFFER_TTL_MINUTES=15

# Email (Gmail example - use an "App Password", not your real password)
# Generate one at: https://myaccount.google.com/apppasswords
EMAIL_USER=anushkashukla7979@gmail.com
EMAIL_PASS=qcnp hjcy qfni azga

# Base URL of frontend (used in waitlist offer email links)
FRONTEND_URL=http://localhost:3000
